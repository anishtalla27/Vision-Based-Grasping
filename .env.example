# Copy this file to .env and fill in real values. .env is gitignored.

WANDB_API_KEY=

# System C's VLM baseline. Must live under its own name, not under
# WANDB_API_KEY: system_b_train.py reads that variable and would try to
# authenticate to Weights & Biases with an OpenRouter key.
OPENROUTER_API_KEY=
